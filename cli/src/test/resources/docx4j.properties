# docx4j configuration for tests
# Disable automatic font detection to avoid issues with system fonts

# Disable font autodetect
docx4j.fonts.fop.util.autodetect.enabled=false

# Use a simple font mapper instead of IdentityPlusMapper
docx4j.fonts.mapper=org.docx4j.fonts.BestMatchingMapper
